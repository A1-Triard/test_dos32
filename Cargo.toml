[package]
edition = "2021"
name = "tstdos32"
version = "0.0.0"
publish = false

[dependencies]
dos-cp = "0.0.1"

[profile.dev]
panic = "abort"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true
